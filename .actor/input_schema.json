{
  "title": "iHerb Product Scraper",
  "description": "Scrape products from iHerb. Provide either keyword/category OR an iHerb URL (startUrl, url, or startUrls). Actor paginates and scrapes product detail pages.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "Multiple iHerb entry points (category, search, or product URLs). Supports objects with `url` or `requestsFromUrl` fields.",
      "editor": "requestListSources"
    },
    "startUrl": {
      "title": "Start URL",
      "type": "string",
      "description": "Start scraping from a specific iHerb URL (e.g., category or search page). If provided, this will be used instead of Keyword/Category.",
      "editor": "textfield"
    },
    "url": {
      "title": "Single URL",
      "type": "string",
      "description": "Alternative single iHerb URL to start from (e.g., product or category). Overrides keyword/category if provided.",
      "editor": "textfield"
    },
    "keyword": {
      "title": "Keyword (if not using Start URL)",
      "type": "string",
      "description": "Product search keyword(s), e.g., 'Vitamin C', 'Protein Powder'.",
      "editor": "textfield",
      "prefill": "vitamin c"
    },
    "category": {
      "title": "Category (optional)",
      "type": "string",
      "description": "Optional product category to filter by (e.g., 'vitamins', 'supplements').",
      "editor": "textfield"
    },
    "location": {
      "title": "Market / Location",
      "type": "string",
      "description": "iHerb domain prefix or full origin (e.g., 'www', 'ru', 'https://au.iherb.com'). Defaults to www.iherb.com.",
      "editor": "textfield"
    },
    "language": {
      "title": "Accept-Language header",
      "type": "string",
      "description": "Custom Accept-Language header value (e.g., 'en-US,en;q=0.9'). Leave empty to use a random desktop locale.",
      "editor": "textfield",
      "default": ""
    },
    "collectDetails": {
      "title": "Collect product details",
      "type": "boolean",
      "description": "If enabled, scraper opens product detail pages for full data extraction.",
      "editor": "checkbox",
      "default": true
    },
    "results_wanted": {
      "title": "Maximum number of products",
      "type": "integer",
      "description": "The maximum number of products to collect. Leave empty to collect all available products.",
      "minimum": 1,
      "default": 100,
      "editor": "number"
    },
    "max_pages": {
      "title": "Maximum number of pages",
      "type": "integer",
      "description": "A safety cap on the number of category/listing pages to visit.",
      "minimum": 1,
      "editor": "number",
      "default": 20
    },
    "maxConcurrency": {
      "title": "Max concurrency",
      "type": "integer",
      "description": "Maximum number of parallel HTTP requests (1-50).",
      "minimum": 1,
      "maximum": 50,
      "default": 10,
      "editor": "number"
    },
    "maxRequestRetries": {
      "title": "Max request retries",
      "type": "integer",
      "description": "How many times to retry a failed request before giving up.",
      "minimum": 0,
      "maximum": 10,
      "default": 3,
      "editor": "number"
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Use Apify Proxy (datacenter recommended for iHerb).",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ]
      }
    },
    "cookies": {
      "title": "Custom cookies (raw header)",
      "type": "string",
      "description": "Optional raw 'Cookie' header string. Can help bypass consent banners.",
      "editor": "textfield"
    },
    "cookiesJson": {
      "title": "Custom cookies (JSON)",
      "type": "string",
      "description": "Optional JSON-formatted cookies. Accepts an array (e.g., [{\"name\":\"a\",\"value\":\"1\"}]) or an object map (e.g., {\"a\":\"1\"}).",
      "editor": "textarea"
    },
    "dedupe": {
      "title": "Deduplicate results",
      "type": "boolean",
      "description": "Remove duplicate product URLs from results (in-memory deduplication).",
      "editor": "checkbox",
      "default": true
    }
  },
  "required": [],
  "additionalProperties": false
}
